<%- include('./partials/header') %> 
<% if (errors) { %>
    <div class="mt-1 errors p-1">
        <ul>
            <% errors.forEach(error => { %>
                <li class="text-yellow-500"><%= error.msg %> </li>
               <% }) %>
        </ul>
    </div>
   <% } %>
<div class="container mx-auto bg-black w-1/2 items-center my-auto h-40">
    <form class="flex flex-col justify-center items-center h-full" action="/join" method="POST">
        <label class= "text-yellow-400" for="secretPass">Enter The Secret Password (HINT: JENNY'S PHONE NUMBER)</label>
        <input class="w-1/2" type="text" name="secretPass">
        <button class="bg-gray-600 w-1/4 m-1">???</button>
    </form>
</div>


<%- include('./partials/footer') %> 